import 'package:flutter/material.dart';

class SettingScreenDetail extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Setting Screen Detail'),
      ),
      body: Center(
        child: Text('Setting screen detail'),
      ),
    );
  }
}
